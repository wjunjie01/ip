public class DukeException extends Exception{
    public DukeException(String errorMessage){
        //Constructor of super class, exception that prints the error message
        super(errorMessage);
    }
}
